# Notification and File Hardening

Notifications are recipient-bound, cursor-paginated and no-store; title/body reject markup and action paths reject traversal/protocol-relative forms. Private file metadata accepts safe names/MIME/bounded size/hash only; storage keys are server-generated under a private actor namespace. No public URL, local filesystem read or external provider assumption exists. File registration is audited.
