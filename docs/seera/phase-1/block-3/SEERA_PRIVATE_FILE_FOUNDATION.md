# Private File Foundation

Stored-file services validate private storage key, name, MIME, positive bounded size and SHA-256 metadata. Uploader and optional entity linkage persist. Access is uploader or `files:manage`/super-admin; no permanent public URL exists and no storage provider is assumed.
