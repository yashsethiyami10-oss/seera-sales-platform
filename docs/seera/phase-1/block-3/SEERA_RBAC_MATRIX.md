# RBAC Matrix

Founder receives all 30 foundation permissions. Company Admin receives governed administration without `system:super_admin`. Each operational role receives only its portal plus minimal notification/file/session rights; delivery is narrower. Auditor receives foundation reads only. Effective permissions union active assignments on active roles; removal increments authorization version and revokes sessions.
