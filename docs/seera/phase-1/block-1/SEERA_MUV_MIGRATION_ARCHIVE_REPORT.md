# Seera MUV Migration Archive Report

## Source and archive

- previous active path: `prisma/migrations/`
- source directories: 60
- source files: 60
- archive: `docs/seera/pre-phase-1/reference/muv-migrations/`
- archive directories/files: 60/60
- manifest: `docs/seera/pre-phase-1/reference/MUV_MIGRATION_MANIFEST.sha256`
- hash differences: 0

Names, ordering and bytes were preserved. The active `prisma/migrations/` now contains only `.gitkeep`; it contains no copied migration SQL.

No migration was generated, validated against a database, applied, resolved, pushed or deployed. The archive is historical read-only evidence and must never be copied back into the active path.

Rollback to the Block 1 start is Git commit `0192067`. Forward work must introduce a new Seera-only initial migration for review; execution remains unauthorized.

