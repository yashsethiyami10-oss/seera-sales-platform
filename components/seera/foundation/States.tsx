import Link from"next/link";import styles from"./Workspace.module.css";export function EmptyState({title,description}:{title:string;description:string}){return <div className={styles.empty}><h2>{title}</h2><p>{description}</p></div>}export function AccessDenied({home,title="Access denied",description="Your role does not permit this workspace."}:{home:string;title?:string;description?:string}){return <section className={`${styles.card} ${styles.danger}`}><h1>{title}</h1><p>{description}</p><Link className={`${styles.button} ${styles.secondary}`} href={home}>Return to dashboard</Link></section>}export function PageHeading({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <header className={styles.heading}><div><h1>{title}</h1><p>{description}</p></div>{action}</header>}
