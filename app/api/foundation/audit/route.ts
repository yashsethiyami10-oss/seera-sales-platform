import { NextResponse } from "next/server";import { prisma } from "@/lib/database/client";import { listAuditEvents } from "@/lib/foundation/audit-service";import { resolveRequestIdentity } from "@/lib/foundation/request-auth";import { apiFailure } from "@/lib/foundation/api-response";
export async function GET(request:Request){try{const{user}=await resolveRequestIdentity();const cursor=new URL(request.url).searchParams.get("cursor")??undefined;return NextResponse.json({events:await listAuditEvents(prisma,user.id,cursor)},{headers:{"Cache-Control":"private, no-store"}});}catch(error){return apiFailure(error,request);}}
