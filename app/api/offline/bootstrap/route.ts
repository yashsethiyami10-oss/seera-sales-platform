import {NextResponse} from "next/server";import {prisma} from "@/lib/database/client";import {resolveRequestIdentity} from "@/lib/foundation/request-auth";import {authorize} from "@/lib/foundation/authorization-service";import {apiFailure} from "@/lib/foundation/api-response";import {offlineBootstrap} from "@/lib/phase-11/offline-bootstrap-service";
export async function GET(request:Request){try{const{user}=await resolveRequestIdentity();await authorize(prisma,{actorId:user.id,permission:"portal:sales_executive",featureFlag:"portal.sales_executive.enabled"});return NextResponse.json(await offlineBootstrap(prisma,user.id),{headers:{"Cache-Control":"private, no-store","X-Offline-Cache-Version":"1"}});}catch(error){return apiFailure(error,request);}}
