import type { PrismaClient } from "@prisma/client";
export type AnalyticsPortal="founder-admin"|"company-admin"|"accounts"|"sales-manager"|"sales-executive"|"distributor"|"super-stockist";
export async function analyticsScope(prisma:PrismaClient,userId:string,portal:AnalyticsPortal){if(portal==="founder-admin"||portal==="company-admin"||portal==="accounts")return{employeeIds:null as string[]|null,partyIds:null as string[]|null};if(portal==="sales-executive")return{employeeIds:[userId],partyIds:null};if(portal==="sales-manager"){const assignments=await prisma.seeraAssignment.findMany({where:{subjectId:userId,assignmentType:{in:["MANAGER_TEAM","TEAM"]},OR:[{effectiveTo:null},{effectiveTo:{gt:new Date()}}]}});return{employeeIds:[userId,...assignments.map(a=>a.targetId)],partyIds:null};}const links=await prisma.seeraPartyUser.findMany({where:{userId,active:true,OR:[{effectiveTo:null},{effectiveTo:{gt:new Date()}}]},select:{partnerId:true}});return{employeeIds:null,partyIds:links.map(l=>l.partnerId)};}
