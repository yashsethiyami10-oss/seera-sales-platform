import {spawnSync} from "node:child_process";import {readFileSync} from "node:fs";import path from "node:path";import {authorizeDatabaseCommand} from "../../lib/database/identity-guard";function envFile(file:string){const v:Record<string,string>={};for(const line of readFileSync(file,"utf8").split(/\r?\n/)){const m=/^\s*([^#][^=]*?)\s*=\s*(.*?)\s*$/.exec(line);if(m)v[m[1]]=m[2].replace(/^['"]|['"]$/g,"");}return v;}const root=path.resolve(import.meta.dirname,"..",".."),prod=envFile(path.join(root,".env")).DATABASE_URL,test=envFile(path.join(root,".env.test")).TEST_DATABASE_URL,target=authorizeDatabaseCommand({intendedRole:"test",write:true,targetUrl:test,productionUrl:prod,testUrl:test}),runtime=new URL(test);runtime.searchParams.set("connection_limit","4");runtime.searchParams.set("pool_timeout","60");console.log(`[SEERA DB GUARD] role=${target.role} host=${target.host.slice(0,4)}***.${target.host.split(".").slice(1).join(".")} fingerprint=${target.fingerprint}`);const result=spawnSync(process.execPath,[path.join(root,"node_modules","vitest","vitest.mjs"),"run","--config","vitest.phase11.integration.config.ts",...process.argv.slice(2)],{cwd:root,env:{...process.env,DATABASE_URL:runtime.toString(),TEST_DATABASE_URL:test,SEERA_DATABASE_ROLE:"test"},stdio:"inherit"});if(result.error)throw result.error;process.exit(result.status??1);
