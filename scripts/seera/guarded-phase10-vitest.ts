import {spawnSync} from "node:child_process";import {readFileSync} from "node:fs";import path from "node:path";import {authorizeDatabaseCommand} from "../../lib/database/identity-guard";
function envFile(file:string){const values:Record<string,string>={};for(const line of readFileSync(file,"utf8").split(/\r?\n/)){const match=/^\s*([^#][^=]*?)\s*=\s*(.*?)\s*$/.exec(line);if(match)values[match[1]]=match[2].replace(/^['"]|['"]$/g,"");}return values;}
const root=path.resolve(import.meta.dirname,"..",".."),production=envFile(path.join(root,".env")).DATABASE_URL,test=envFile(path.join(root,".env.test")).TEST_DATABASE_URL,target=authorizeDatabaseCommand({intendedRole:"test",write:true,targetUrl:test,productionUrl:production,testUrl:test}),runtime=new URL(test);runtime.searchParams.set("connection_limit","2");runtime.searchParams.set("pool_timeout","60");console.log(`[SEERA DB GUARD] role=${target.role} host=${target.host.slice(0,4)}***.${target.host.split(".").slice(1).join(".")} database=${target.database} fingerprint=${target.fingerprint}`);const cli=path.join(root,"node_modules","vitest","vitest.mjs"),result=spawnSync(process.execPath,[cli,"run","--config","vitest.phase10.integration.config.ts"],{cwd:root,env:{...process.env,DATABASE_URL:runtime.toString(),TEST_DATABASE_URL:test,SEERA_DATABASE_ROLE:"test"},stdio:"inherit"});if(result.error)throw result.error;process.exit(result.status??1);
